include ../user/user
include ../title/title

mixin menagers-block(options = {})
    -
        const {data = [], title = ""} = options;
    section.menagers-block
        .menagers-block__title
            +title({text: title})
        .menagers-block__el
            for item in data
                +user(item)