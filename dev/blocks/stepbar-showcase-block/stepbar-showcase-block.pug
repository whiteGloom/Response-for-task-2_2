include ../title/title
include ../stepbar/stepbar

mixin stepbar-showcase-block(options = {})
    -
        const {stepbarData = {}} = options;
    .block
        .block__title
            +title({text: "Stages", level: 3})
        .block__el.block__el_margin_big
            +stepbar(stepbarData)