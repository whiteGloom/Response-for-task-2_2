mixin chart(options)
    -
        var options = options ? options : {};
        var {id = "", size = "fixed", type = "progress"} = options;
    .chart&attributes(attributes)
        .chart__outer(id= id class="" + (" chart__outer_size_" + size) + (" chart__outer_type_" + type))