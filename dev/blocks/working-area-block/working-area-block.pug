include ../title/title
include ../paragraph/paragraph

mixin working-area-block(options = {})
    -
        const {data = [], title = ""} = options;
    .working-area-block
        .working-area-block__title
            +title({text: title})
        .working-area-block__el
            for item in data
                +paragraph({text: item.text})