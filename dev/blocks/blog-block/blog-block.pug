include ../article/article

mixin blog-block(options = {})
    -
        const {data = []} = options;
    .block
        .block__title
            +title({text: "Блог"})
        .block__el.block__el_margin_normal
            +article({data: data})
